import {OpeningItem} from '../../../../../Models/DrawModels/Openings';

export interface OpeningComponentProps {
  item: OpeningItem;
}
const openingSnappingThreshold = 200;
const useOpeningComponent = ({item}: OpeningComponentProps) => {
  return {};
};
export default useOpeningComponent;
