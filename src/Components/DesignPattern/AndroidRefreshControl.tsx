import {RefreshControlProps, StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = RefreshControlProps & {};

const AndroidRefreshControl = (props: Props) => {
  return (
    <View>
      <Text>AndroidRefreshControl</Text>
    </View>
  );
};

export default AndroidRefreshControl;

const styles = StyleSheet.create({});
